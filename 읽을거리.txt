이야기1:

 처음은 Paradox사의 게임 EU3(유로파유니버셜리스3) 라는 세계정복? 게임으로 부터.. 시작이었습니다.

​

단순히 세이브파일데이터를 수정해보기위해서 간단한 파서를 제작하게되었습니다.

​

그래서 만들어진 레포?가 ClauParser이구요(EU3는 클라우제비츠엔진으로 만들어졌습니다.)

​

세이브파일 형식이 더 자유로운 json 형식이라고 생각하시면 됩니다.

​

Array와 Object의 구별이 없으며 모두 { } 로 표시하고 때때로 Mixed(array도 아니고 object도아닌) 도 있었던걸로 기억합니다.

​

그리고 key값이 따옴표로 둘러쌓여있지 않을 수 있습니다.

​

이야기2:

 지금 제가 만들고있는 json파서(vztpv/scj3, claujson) 는 내부적으로 simdjson을 수정해서 Scanner로써 사용하고 있습니다.

​

 그리고 문법체크와 AST구성을 분리해서 각각 병렬처리(std::thread을 이용) 합니다. 이렇게하면 AST구성을 할 때

 

이야기1의 ClauParser에서 사용했던 병렬 알고리즘을 (개념적으로???) 거의 비슷하게?? 쓸 수 있습니다. 

 

이야기3:

 std::thread를 이용해서 병렬 파싱(parsing)을 하기 때문에 토큰 배열이 필요합니다. 

​

여기에 simdjson을 수정한것으로부터 토큰 배열을 가져오게됩니다.

​

토큰배열을 콤마(,) 기준으로 여러개로 나눈다음, 나눈 부분별로 각각 동일한 함수를 호출해서 돌아가게 됩니다.

​

문법체크를 할떄도 콤마를 기준으로 하고 있습니다. 제 나름대로 문법체크를 해놨지만, 문법체크가 맞게되어있는지는 

잘모르겠네요 ㅠㅠ (부족한 부분이 있을것같긴합니다.)

​

파싱을 할떄도 콤마를 기준으로 나눕니다.

​

이야기4:

 파싱함수는 { 만나면  Object클래스를 만들고, [를 만나면 Array클래스를 만들고.. 간단합니다.

​

다만 다른 파서들과 차이점이라면... Virtual Array, Virtual Object (virtual이라고 이름붙인 것들입니다.)

​

내부적으로 파싱등을 할때에만 사용됩니다. 토큰배열을 여러등분을 했기때문에 당연하게도 문법적으로 맞는 않는 토큰들을? 파싱해야합니다.

​

다음과 같은 경우가 생깁니다.

​

    "test" : 123, "test2" : 456 } ] ]  

​

일단은 문법체크를 통과해야 파싱을 하기때문에, 이를 이용해서.. 현재 array에 속해있는지 , object에 속해있는지를 

​

쉽게 알 수 있습니다.

​

문제는  } ] ] 이겠죠... } 를 만나면 Virtual Object를 만들어줍니다. 그리고 데이터를 적절하게 옮겨줍니다.

("test":123 등등)

​

그리고 ] 를 만나면 Virtual Array를 만들어줍니다. 

​

잘생각해보면...  Virtual Array나 Virtual Object는 0번째에만 생깁니다. 

​

그리고 nested하게 만들어질 수 있습니다.

​

stack이나 parent를 이용하면... 현재 element?가 속한 Array나 Object를 저장할 수 있습니다.

​

결과적으로 파싱함수가 끝난후에 마지막 element가 속한 Array나 Object정보(parent포함) 및 다음? 부분? AST의 Virtual Node(Array or Object)를 이용해서 2개의 부분 AST를 합칠수있습니다.(Merge)

​

 Merge할때에는 0번과 1번을 한후 0번과 2번, 0번과 3번...이런 느낌으로 하도록 일단은 만들었고 

​

Merge하는데 시간이 많이 걸리지 않기 떄문에 (파싱이 300ms이면 Merge는 10ms이내) 현재까지는 좀더 개선을 하는것을 신경쓰지않고있습니다.

​

 0번과 1번을 합칠때를 생각해보면..

​

0번의 마지막 요소의 위치와 1번의 virtual node의 가장 안쪽부분과 node의 get_parent()함수를 이용하여

​

안쪽에서부터 (parent로) 올라가면서 데이터의 이동이 일어나게 됩니다. 
